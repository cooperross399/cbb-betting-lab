# Local development only. This file is an EXAMPLE and carries no real value.
# The production credentials are the GitHub secrets CBB_ODDS_API_KEY and
# CBBD_API_KEY. A real .env is gitignored and must never be committed.
#
# Same Odds API account and the same credit pool as the NHL and football labs.
# Spending here spends there.
CBB_ODDS_API_KEY=
# CollegeBasketballData bearer token. Free self-service, separate quota.
CBBD_API_KEY=
