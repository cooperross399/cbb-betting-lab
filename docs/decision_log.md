# Decision log

Every question this build wanted to ask Cooper and answered itself, with the
answer and the reasoning, one line each. Cooper's instruction: *"That is how I
audit the autonomy afterwards without being interrupted during it. A build that
made two hundred judgment calls and recorded them is one I can review; one that
made them silently is one I have to re-derive."*

Newest last. Dates are the day the call was made.

| # | Date | Question | Decision | Why |
|---:|:---|:---|:---|:---|
| 1 | 2026-08-31 | The brief says the repo is `cbb-betting-lab` under `~/Projects`, but two copies of the brief sit beside it (`cbb-betting-lab-brief.md`, `cbb-betting-lab-prompt.md`). Which is `docs/original_brief.md`? | The `-prompt.md` copy. | It is the text Cooper actually sent, verbatim. The `-brief.md` copy wraps the same content in "how to use this file" scaffolding aimed at a human starting the session, which is not the authorisation a future session needs to read. |
| 2 | 2026-08-31 | The local `.env` in the EPL lab holds an Odds API key. Use it? | No — it is dead. | A `/v4/sports` call returned HTTP 401 `INVALID_KEY`. Proven rather than assumed, per the brief's instruction to read the environment rather than the error text. The live credential exists only as a GitHub secret, which is write-only by design and cannot be read from this machine by any means. Cooper set `CBB_ODDS_API_KEY` on the new repo unprompted at 00:40 UTC 2026-09-01, which resolves it: every credit-spending step runs in Actions, where the secret is available, and that is the architecture the brief asks for anyway. |
| 3 | 2026-08-31 | The sibling labs call their registry `leagues.py`. College basketball has no "league". | `competitions.py`, one entry, `Competition`. | The brief calls it "the competition registry" and that is the honest noun. The shape is the football lab's `League` dataclass unchanged, so the port stays mechanical. |
| 4 | 2026-08-31 | What is a game's date, when a 22:30 Pacific tip is 01:30 Eastern the next morning and 06:30 UTC the morning after that? | The **slate day**, 06:00 ET to 06:00 ET. | The NHL lab lost 69% of its bought prices to a UTC date meeting a league date, and the survivors were systematically the afternoon games. Here the exposure is worse: the late window is where the low-major games live, and the low-major end of the board is this lab's entire thesis. The boundary is not asserted — `tests/test_slate_day_and_season_match_the_source.py` (this row was written when the boundary was 06:00 ET; it was later measured to zero and the file above records both; the citation here used to name a test that never existed) checks it against the source's own game date over every cached game. |
| 5 | 2026-08-31 | Should `Hypothesis` allow a missing predicted direction, as the football lab's does? | No — it raises. | That lab recorded the defect itself: "three of the twelve hypotheses were written with no predicted direction, so they could not be falsified by direction and three slots were spent on cuts that could only ever be exploratory". Four of its mechanisms reversed outright, and knowing they reversed was worth more than the null result — which is only sayable because a direction was written down first. |
| 6 | 2026-08-31 | The football lab's verdict for one policy flipped depending on which season had been run last. Carry that shape? | No. A verdict records `seasons_tested` and `seasons_cleared`, and `ships()` is false unless it cleared **all** of them. | Same policy, same script, opposite verdicts, and the card reads whichever ran most recently. Comparing two overlapping intervals and taking the larger number is how a lab ships noise. |
| 7 | 2026-09-01 | Which futures sport keys exist for college basketball? | `basketball_ncaab_championship_winner`, and only that. | Read from the provider's own `/v4/sports?all=true` listing rather than assumed. There is no conference-winner key and no season-win-totals key at the sport level; if those markets exist they are market keys under `basketball_ncaab`, and that is an in-season probe question rather than a September one. |
| 8 | 2026-09-01 | How is women's basketball kept out of the population at the provider? | It cannot get in: the provider serves it as a **separate sport key**, `basketball_wncaab`. | Verified in the same listing. The purity guard is therefore cheap and total on the price side — a test pins that no fetch in this repository ever names `basketball_wncaab`, `basketball_wnba` or any key but the two the registry declares. The harder half is the results side, where ESPN ids and NCAA pages do mix divisions and sexes; that is guarded separately. |
| 9 | 2026-09-01 | Cooper, mid-build: *"dont worry about women's basketball"*. Does anything change? | Nothing is built for it; the one-test purity guard stays. | It confirms the brief's scope rather than narrowing it. The guard is not a women's-basketball feature — it is a test that no fetch names a sport key the registry did not declare, and it costs one file. Removing it would not save work; it would only remove the thing that notices if a `basketball_wncaab` key or a women's ESPN id ever reaches the population by accident. |
| 10 | 2026-09-01 | CI was red on `main` for two pushes and the repo had no `tests.yml` at all. Add one that fetches the data cache so every step can run? | No. CI proves the suite, not the cache. | The cost arithmetic and the probe's dry run both read a gitignored 460MB cache. Fetching it in CI would put an external release host (sportsdataverse) on the critical path of a gate that must stay green for reasons internal to this repository — an outage there would go red here and mean nothing. Dryness is instead proved at the socket layer by `tests/test_the_dry_run_is_dry.py`, which blocks `connect()` and hides the credential, and is a stronger claim than grepping stdout for a sentence. |
| 11 | 2026-09-01 | The retention probe failed twice in Actions for missing data. Let it fall back to the raw schedule when `cbb_team_games.csv` is absent? | No. The workflow builds the table. | The probe draws its population from the same `game_state` the backtest does, precisely so the two can never disagree about which games count. A fallback would make them diverge silently — which is the class of defect that shared table exists to prevent — and it would do so in the direction that looks like success. The runner fetches and builds, and caches the result for the purchase runs behind it. |
| 12 | 2026-09-01 | The gameday workflow referenced three scripts that do not exist (`run_gameday_card.py`, `run_forward_evidence.py`, `run_what_we_can_claim.py`). How was this not caught? | Nothing had dispatched it, and no test read the workflow. Added `test_every_script_a_workflow_runs_actually_exists`. | A green workflow file is not a workflow that works — the EPL lab spent five days green and empty. The whole delivery chain sat behind three filenames that resolved to nothing, and the cheapest possible check for that is a regex over the workflow files. Written as a failing test first, then satisfied. |
| 13 | 2026-09-01 | The full historical catalogue costs 35,173,680 credits against a balance of 4,992,714. Buy a sample of everything, or all of the cheap thing? | All of the cheap thing, in the brief's stated priority order: core team markets across every season first. | Seven times the balance is not a near miss to be optimised away. The provider retains featured markets from 2020-11-16 and everything else only from 2023-05-03, so the deepest population this lab can ever have is also its shallowest catalogue: 32,379 games of moneyline, spread and total against three seasons of props. Cooper's case for a fourth lab was sample size, and the arithmetic says he is right about the team markets and wrong about the props. Recorded in `docs/credit_cost.md`. |
| 14 | 2026-09-01 | The probe's pessimistic bound is 147,020 credits — twenty times the football lab's whole probe. Cut it? | No. Run it at 144 events across all three tiers. | It is 2.9% of the remaining balance, spent to decide how the other 97% is spent, and the football lab's probe is not the comparison: that one asked "does the archive retain this market" for one population. This one asks it **per conference tier**, which is the brief's entire thesis — *"If the market's efficiency is not uniform across the board, this is where that shows up."* A probe stratified only by month would answer a question nobody asked. |
| 15 | 2026-09-01 | `scripts/run_forward_evidence.py` writes to a store that can only grow and cannot be rebuilt. Should a bare invocation settle? | No. The default is `--report-only`; settling needs `--settle`. | Same shape as `run_retention_probe.py` being dry until `--live`. There the accident costs credits; here it costs a settle pass run against a half-published box score, whose verdicts land in an append-only ledger nothing can revise. The workflow passes `--settle` explicitly, so the safe default costs nothing. |
| 16 | 2026-09-01 | A settle pass with a missing processed table or a missing cached schedule does not crash — it settles the whole night as "no game in the results tables matches". Degrade, or refuse? | Refuse, exit 2, before the first `.settled` marker is written. | It is the failure that succeeds quietly and wrongly. An empty team-name index resolves no fixture, so a night recorded that way is indistinguishable from a night that was never played — except it is permanent once the 14-day patience window closes. Note the deliberate divergence from `reports/gameday_card.py`, which falls back to an earlier season's schedule: a card can be re-made tomorrow and a settled ledger row cannot. |
| 17 | 2026-09-01 | An unreadable snapshot (the football lab's zero-byte `git show` artifact). Red run, or warning? | Named in a `::warning::`, counted in the reconciliation, exit 0. | It is a permanent fault no rerun repairs. Exiting red would mark every remaining run of the season degraded, which stops the gameday backup trigger ever standing down and spends a second slate's credits every day for a file nothing can fix. The rest of the archive settles around it, which is the brief's rule that a run degrades rather than empties. |
| 18 | 2026-09-01 | `forward_evidence.SETTLEMENT_AMBIGUOUS_MARKETS` defaults to empty — the module says deciding which of a lab's numbers are not evidence is the caller's job. What does this caller decide? | Pass the whole second-half family as settlement suspects, every run, with no flag to turn it off. | Second-half wagers settle including overtime at most US books and not all of them. That is a book's rule, this lab holds no rulebook, and those rows therefore measure the settlement rule as much as the model. Marking them **not evidence** at any sample size is the conservative direction, and a flag to unmark them would be a gate with an off switch. |
| 19 | 2026-09-01 | `forward_evidence.read_ledger` is lenient: a damaged CSV reads back as zero rows. Good enough for the report? | No. Both modes read the ledger through `stores.read_store(..., for_append=True)` and stop on `CorruptStoreError`. | The report is published to `card-feed` as `latest_forward_evidence.md`, where it is the only copy anybody reads. A damaged ledger read leniently publishes "0 frozen opinions" over a season of them and looks completely healthy — the most misleading output this lab could produce. A file that is merely absent is still zero rows, because that is a fresh clone rather than damage. |
| 15 | 2026-09-01 | The `schedule` skill reported *"No available MCP connectors found"*, which would mean the Drive relay cannot be built at all. Accept that and report the chain as impossible? | No — read the connector off the working `EPL CARD RELAY` routine instead. | The skill was describing this session's view, not Cooper's account. `RemoteTrigger list` shows `EPL CARD RELAY` carrying a `Google_Drive` connection with its uuid, url and transport, and reusing the connector he already authorised is the difference between a routine that works on its first fire and one that asks him for a second thing. The brief's instruction is to read the environment rather than the error text; the error text was wrong. |
| 16 | 2026-09-01 | Creating `CBB CARD RELAY` returns 403 on repository access. Work around it by having the routine fetch the card over HTTPS with a token, or by making `card-feed` public? | Neither. Record it as the one setup click and keep building. | A token in a routine prompt is a credential in a place this lab has spent a great deal of effort keeping credentials out of, and making the feed public publishes Cooper's card to the internet to save him a click. The brief anticipates exactly this case — *"if obtaining it needs me, that is the one kind of thing worth a single line in the final report, not a stop"* — and the body is committed ready to create, so the click is the whole of the work. |
| 20 | 2026-09-01 | `models/ratings.py` does not exist, so `scripts/run_price_backtest.py` has no model to price with. Build a pricer inside the script so the backtest can run today? | No. `--model module:attribute`, defaulting to `cbb_betting_lab.models.ratings:matchups_for`, and a non-zero exit when it cannot be imported. There is deliberately no fallback. | A backtest that quietly prices with something other than the model the card runs measures a policy nobody would have run, and it prints intervals while doing it. The default points at the name the rest of the repository already uses — `gameday_card.opinions_for`'s own docstring says *"no rating exists for this game — `models/ratings.py` is not written"* — rather than inventing a second one for the measurement side to drift from. |
| 21 | 2026-09-01 | Should the backtest read a market's probability off a `GameDistribution` itself, or go through the card's own reader? | Through `gameday_card.opinions_for`, and the join key through `card_pricing.default_key_for`. | Two pricers is the football lab's ladder defect: it priced its featured spread from one model and its alternate rungs from a normal approximation to that model, and shipped a −6.5 that was better value than its −7.5, with nothing in the output looking wrong. A backtest reading markets by hand would be free to disagree with the card about what the card's own opinion was, and the disagreement would be invisible in both outputs. The model contract is therefore only what `opinions_for` needs — one matchup per event. |
| 22 | 2026-09-01 | `card_pricing.PRICE_BAND` (−400 to +600) and the one-position-per-game exposure cap are bars a real card applies. Apply them to the backtest's bets? | No. `price_backtest.bets_from` is the declared cut and nothing is added to it; the count of bets outside the band is printed as a diagnostic instead. | Applying them would be a second, undeclared cut inside the measurement, decided after the module that owns the numbers had already declared one. Printing the count keeps the fact visible — *the largest apparent edges in any price store are the rows that are wrong*, and they all live outside the band — without moving a threshold this repository is arranged to keep fixed. |
| 23 | 2026-09-01 | Zero graded bets. A finding, or a fault? | Both, and they are told apart: **no opinion on anything** exits non-zero and names the model's own reasons; opinions that simply did not clear the threshold exit zero and are reported. | The football lab's props backtest reported zero bets and had it read as "the model never disagrees enough with the market" when in truth its price columns had never been built. The accounting identity is what separates the two cases, so it is printed every run and the run stops when it does not reconcile. |
| 24 | 2026-09-01 | Key numbers are *measured from the games supplied*. Which games — the whole fitted population, or the ones the store covers? | The games the price store covers, one row per game rather than per team-game. | The decomposition asks how much of *this store's* edge is half a point at a number, so the number's frequency has to be a fact about the same population. Home and away rows carry the same total and equal-and-opposite margins, so counting both would make every margin distribution symmetric by construction — a shape that is an artifact of the table rather than of the sport. |
| 25 | 2026-09-03 | The card relay routine cannot be created — HTTP 403, the cloud environment has no access to this repository. Is that really Cooper's to fix, or can Claude grant it? | Cooper's, proven. | The brief says to prove something needs him before saying so. `gh api /user/installations` returns 403: modifying a GitHub App installation needs a user-to-server token issued by that App, and the OAuth token here (`repo`, `workflow`, `gist`, `read:org`) cannot mint one. No route around it exists from this environment. Recorded in `docs/delivery_chain.md` with the exact remedy. |
| 26 | 2026-09-04 | The brief authorises 1,500,000 credits per calendar month and September's spend reached 1,491,848 with props, futures and most of the ladders unbought. Stop, or keep buying? | **Neither, until Cooper said so.** The wave-2 purchase was capped at 100,000 to land exactly on the authorisation, and the rest was queued for October per the brief's own instruction — *"buy in priority order and continue next month rather than stopping"*. Cooper then lifted the ceiling explicitly (*"lift the ceiling and buy the rest"*), which is the authorisation this row records. | A two-word instruction does not silently override a written number, so the ceiling was honoured until it was raised in as many words. The provider quota was never the constraint — 3,508,152 remained — Cooper's authorisation was, and the two are different things a report must not confuse. Headroom is still reserved for the NHL and football labs' committed spend, because that constraint was not lifted. |
| 27 | 2026-09-05 | Run 33917619764 spent 1,199,926 credits on the ladders-and-halves wave and persisted nothing (defect S). Re-buy it now? | **No — Cooper's call, and reported plainly.** The provider quota is 1,118,479, which cannot cover a 1.2M re-buy this month in any case; more to the point, re-spending a seven-figure sum to repair my own workflow defect is not a decision to make unprompted. What survives is rebuilt for free and re-measured; the ladders-and-halves population is the 609 events of the first small run until Cooper decides. | The brief lifts the credit ceiling on Cooper's word, not on mine, and a loss caused by the build is exactly the kind of thing that has to be put in front of him with the number attached rather than quietly recovered. |
| 28 | 2026-09-05 | `--write-graded` exported `PB.settled(bets)`, so the market-vs-model regression, the Brier comparison and the claimed-edge buckets were fitted on the threshold-selected bets — selected by the model's disagreement with the price, then regressed on that same disagreement. Add a second export flag, or change what the one export means? | Change it: the export is now **every settled wager the model had an opinion on**, with a boolean `selected` stamped by the same `bet_mask` predicate `bets_from` uses. `forecast_skill` fits every opinion as the primary population and reports the selected subset **beside** it, labelled in words as the winner's-curse comparison and never as the skill measure; `build_skill_frame.py` refuses a frame without the flag. Record version bumped to 2. | A regression over the bets alone has the winner's curse in its coefficient and a bucket table with nothing below the threshold by construction — the brief's item 13 asks for the regression over every candidate. A second flag would leave the old one available and indistinguishable in a stale `cbb_graded_bets.csv`, which is why the frame builder refuses the unflagged shape rather than passing it through. |
| 29 | 2026-09-05 | `run_replication.record_holdout_looks` appended a holdout hypothesis only where discovery had claimed, yet the replication scores every discovery cell on the holdout: N looks taken, K recorded. The unclaimed cells carry no sign — skip them, record two one-sided looks each, or admit an undirected hypothesis? | One two-sided hypothesis per unclaimed cell, `predicted_direction="either"` (`experiment_ledger.TWO_SIDED`), **admitted at the holdout stage only**. The discovery stage still raises `DirectionRequired` on anything but `higher`/`lower`. A two-sided look can fail but cannot reverse. | Every interval this lab quotes is two-sided, so one holdout cell read is one look whether or not a sign was carried in; recording two one-sided entries would double-count, recording none undercounts the family by exactly the looks nobody wrote down. The discovery-stage refusal is decision 5 and is unchanged: a two-sided *pre-registration* is the football lab's exploratory slot, while a two-sided *holdout read of a cell that claimed nothing* is a look that was genuinely taken and has to be counted. |
| 30 | 2026-09-05 | `build_skill_frame.py` refused to write when any graded bet lacked its complement, and on the full store 2 of 566,377 rows (0.00035%) have none — the whole measurement stopped over two rows. Both are real: betmgm filed one alternate_spread pair with **both** sides at +18.5 on 2024-02-17, and draftkings hung `home_over` 74.5 with its `home_under` only at 73.5 on 2024-11-24. Neither missing side is in the store at all and `stores.dedupe_prices` dropped nothing (3,863,325 rows in, 3,863,325 out). Keep refusing, or drop the two rows? | **Neither.** A one-sided quote holds no vig, so those wagers genuinely cannot be de-vigged and excluding them is the only honest arithmetic — but *silently* was the whole objection, not *excluding*. `build()` now returns an `UnpairableCensus`: `supplied = paired + unpairable` reconciled from two independent counts, broken down by market, book and tier, with the excluded rows named and the threshold-selected ones counted apart, printed in the shape `OpinionAccounting` prints its identity and written to `cbb_skill_frame__unpairable.json`. `forecast_skill` reads that census and states the excluded count wherever it states a population, and says *not supplied* — never *none* — when no census came with the frame. Record version bumped to 4. Above `MAX_UNPAIRABLE_SHARE = 0.0001` it still refuses. | The threshold is placed between the two things it must tell apart, both measured on the real export: the observed quirk is 0.00035%, so 0.01% leaves 28x headroom before the lab stops again over rows that never carried a hold; and a join that breaks loses a *slice*, not a scattering — the smallest single book in the graded export is 0.021% of it and the smallest single market 0.052%, so a break confined to one entire book is still twice the threshold and still refuses. A share and not a row count, deliberately: on a frame where one unpairable row exceeds it, one row really is a large share of that measurement and nothing distinguishes a quirk from a break at that size. The census is what makes the exclusion reviewable — a reader who sees only the frame's length would take it for the graded set, and it is now 2 rows short of it. |
| 31 | 2026-09-05 | The `UnpairableCensus` identity was a tautology — `paired` was `len(graded) - unpairable` — and 7.9% of the price store's total-side rows (146,617 of 1,850,597) do not pair at their own book, far above anything the graded side shows. Is the store number a reason to distrust the pairing rule? | **No, and it is now written down rather than left open.** The identity is fixed first: three terms, each counted off the frame, plus a cross-check on the complement block and one in `main` against the rows it read. The store's 7.9% is then measured, not assumed: 146,612 of the 146,617 (99.997%) are player props, all in the 2024 season, 146,523 of them the `over`, and 141,664 have the same book quoting the other side at a different number. The graded export carries **0 player-prop rows of 566,377**, so not one of them is ever asked for as a complement. The remaining 5 are draftkings `team_total` quotes, one of which is already a named excluded row. | A number that large deserved an answer and not a shrug, and the answer had to be a count. Books hang props one-sided and at asymmetric numbers as a matter of course; that is a fact about prop pricing, not a failure of `pair_key`, and the handicap side — 28 unpaired of 2,012,728, 0.0014% — says the key itself is fine. It is explicitly **not** headroom for the refusal threshold, which is read against the graded share (2 in 566,377) and nothing else. Written into the module docstring so the next reader does not re-derive it from a 977MB file. |
| 32 | 2026-09-05 | Two sessions used this checkout on 2026-09-04: another one, under Cooper's login, checked out `fix/ci-guards-hold` from `main`, committed #6, opened the PR and left HEAD on its branch. My next commit landed on that branch, `git checkout -b` made #7 a child of #6, and `git add -A` on the amend swept in a 126-line `tests/conftest.py` continuation the other session had left uncommitted. Treat as an accident, or as a rule? | **A rule.** Before any branch or commit: `git checkout main && git fetch origin main && git reset --hard origin/main && test -z "$(git status --porcelain)"`. A dirty tree or an unexpected HEAD stops the work until it is understood. #7 was closed (branch kept, so the other session's conftest work is recoverable from its head 0ba0f12) and re-opened as #8 from a clean parent via cherry-pick, not force-push. | The brief forbids force-pushing and two sessions in one working tree is exactly how a commit ends up on the wrong parent with someone else's unreviewed work inside it. The rule costs four commands and would have prevented both. |
| 33 | 2026-09-05 | PR #6 measured that a clean clone — the CI runner — ran `main` as 647 passed, **80 skipped**, every skip waiting on a gitignored table. Trust the PR body, or reproduce it? | **Reproduced**, in a throwaway worktree at 02e75b7 with no `data/`: **1 failed, 647 passed, 80 skipped.** So every "CI green on main" reported before #6 landed was green on a suite that never ran the settlement proofs, the provider-name resolution, the slate-day check or the cron-lateness arithmetic in CI. #6 committed fixtures and a zero-skip junit gate; on current `main` a clean clone runs 1241 passed, 0 skipped. The one failure was `test_no_allowlisted_market_means_no_selection_and_a_stated_reason` — the card's own honesty guard tripping on the word `lean` outside a denial in the no-board rendering — present on the pre-#6 tree without data and not reproduced on current `main` with fixtures. Noted, not chased: the zero-skip gate now surfaces it if it recurs. | "Prove it before saying so" applies to a claim that undercuts my own earlier claims most of all. |
| 34 | 2026-09-05 | The Ledger Guard was red on the `push` event and green on `pull_request` for #6, #7 and #8 — every PR, one failure and one success. Flaky, or by design? | **By design and still wrong.** A branch's first push carries an all-zeros `github.event.before`; the guard could not resolve it and exited 1 with "this is a broken guard, NOT evidence the ledger is intact". Correct in spirit; the consequence is a check that is red on every new branch, which is a check people learn to ignore. PR #9 makes a first push compare against `origin/main` — for a new branch every commit is after `main` by definition — while a real `before` on a later push is still used and an unreachable `main` still fails loudly. Three tests run the real step text under #6's stub harness. | A guard that cries wolf on schedule stops guarding. |
| 35 | 2026-09-05 | The first free rebuild after fixing defect U reported 2,946,929 rows from 31,675 events — exactly the core-team-only figure — with `props/2025: 0 rows from 0 cached events`. Was U's fix wrong? | **Right, and defeated one layer up.** `--waves all` rebuilds from every wave's cached responses *on disk*, and the cache **restore** was still wave-scoped: its first key `cbb-bought-<window>-<wave>-` hit for any wave bought before and shadowed the any-wave fallback, so a run dispatched as `core_team` restored core_team's files only. The props cache — saved on top of everything before it — held the complete surviving set. Recovered by dispatching the rebuild as `props` (a free run; the prefix hits the complete cache). Fixed durably by a per-run save key and a single any-wave restore key, so every run starts from the whole surviving set. Defect W. | I asked for the "all waves or one?" check before the run finished and the log was not readable in flight; the answer arrived with the completion. A verification you cannot read in time is not a verification, so the fix is structural rather than a habit of checking. |
| 36 | 2026-09-05 | PR #11 opened red, its first commit carrying one of four intended files. How? | **A heredoc terminator mid-chain breaks `&&` gating.** In-line `python3 - <<'PY' … PY` edits failed on the second file (variable named `ledger`, not `ledger_path`); the statements after the `PY` line — `git rm`, the commit, the push, `gh pr create` — were new statements, not links in the chain, so the suite's failure stopped nothing that mattered. Rule: multi-line edits go in a script file run under `bash -euo pipefail`; a heredoc never sits inside a chain that is supposed to gate. Applied the same hour to finish #11. | The gate I trusted was not a gate. A test that cannot fail and a chain that cannot stop are the same defect. |
| 37 | 2026-09-05 | The 609-event ladders run survived only in its own cache lineage and a 90-day artifact; the per-wave cache chain never restored it, so the free full rebuild lacked it. Re-buy, or recover offline? | **Recover offline.** Both runs' raw-cache artifacts (ladders 27,867 files, props 53,974) were downloaded, merged into one cache directory and rebuilt locally with `--rebuild --waves all`, spending nothing. Two slips on the way, both recorded: `upload-artifact` strips the common ancestor, so the artifact root *is* `card/`'s contents and `find -type d -name card` finds nothing; and `[ -n "$d" ] && cp …` with an empty `$d` is invisible to `set -e`, so the first merge "succeeded" with zero files and its `rm -f` had already deleted the staged store. The pipeline re-checks the store at fire time and refuses below the props-only count, which is why that cost nothing but time. | The responses are the purchase; every copy of them is worth a rebuild attempt before a single credit is re-spent. |
| 38 | 2026-09-05 | The staged full store holds 504,394 props rows where the provider returned 535,475 quotes, and the census was read against a "535,475" that turned out to be the raw count, never a store count. Reconciled to the row: the registry folds each `_alternate` ladder onto its base market by design, a ladder repeats the main line, and `stores.dedupe_prices` keys on the quote (event, market, segment, player, selection, line, book, phase) — so 31,065 identities were quoted more than once by the same book, 30,769 at the same price and 296 at two prices. `keep="first"` kept the worse of the two on 189 quotes (0.04% of props rows), median gap 0.70 pp of implied probability, one outlier of 31.22 pp. Fix before or after the measurement? | **Record now, fix after.** The measurement runs on the staged store as it is; a dedupe that keeps the best price per identity (sort by payout before `drop_duplicates`) is a one-line change with a test and lands as its own PR once the docs batch is on `main`, so the measured store and the code that built it stay the same thing. The magnitude is stated so nobody has to take "negligible" on trust. | A store the reports were run on and a store the code would build today must be the same store, or the record describes something that no longer exists. 189 quotes at 0.70 pp cannot move a verdict; the sentence saying so is what makes that a claim rather than a hope. |
| 39 | 2026-09-05 | The six-season measure ran for 3 h 11 m and a profile showed 88% of it inside one function: `_match_variance` solved a 60-step bisection per distribution node, 1,066,772 inner calls per 45 games. Optimise a numeric hot path before the measurement, or measure first and leave it alone? | **Optimise, but only against proof of equivalence.** The previous scalar implementation stays in the module as `_match_variance_reference`, and three separate checks were required before the change could merge: an AST comparison showing the kept reference is character-for-character main's code, property tests over 1,040 random and boundary nodes plus 7 realistic `build()` parameter sets, and a full 30-day, 1,426-event backtest whose record was compared leaf by leaf against a baseline built from the old code. 4,341 leaves, maximum float difference 0.0. 767 s became 237 s. | A performance change to a measurement is a change to the measurement unless equivalence is proven, and the property tests alone would not have been enough: before an exactness fix, 5% of nodes differed by 2.3e-15 and one distribution changed **shape**, because a 1e-15 trim cutoff sits exactly on FFT-noise columns. Only the end-to-end record comparison caught that. |
| 40 | 2026-09-05 | An audit of this repository against the brief (nine dimensions, every finding adversarially verified, unanimous survival required) returned 27 confirmed findings from 88 candidates, ten of them high severity, including three on the in-season card path. Fix them before the full-store measurement, or measure first and fix after? | **Fix first, then measure.** Two of the findings changed what the measurement counts: an unresolved player name was graded as a void rather than as unknown, and the market-versus-model regression was fitted on the model's own selections. A third undercounted the family-wise correction by recording holdout looks only for cells that had claimed in discovery. Measuring before those landed would have produced a record that had to be thrown away. | A number is only worth the code that produced it. The evidence: the full-store run now reports 11,439 unsettleable wagers, of which 11,181 are names that resolve to nobody on either box score — every one of those was previously counted as a settled non-loss. |
| 41 | 2026-09-05 | The measurement pipeline had been running for 35 minutes when six further fixes landed on `main`, among them the renderer's sample-size labels, what the accounting identity actually proves, and the shape of the forecast-skill record. Let it finish, or kill it and restart on the new head? | **Kill it and restart.** The run was reading code seven commits behind `main`. A record has to come from the code the repository carries, or it describes something that no longer exists — the same principle recorded above for the best-price dedupe. The restart cost 35 minutes of a 62-minute pass. | The alternative was a record whose reports would have to be re-rendered from code that never produced them, and a claims document generated by a generator `main` had already replaced. Thirty-five minutes is cheap next to a measurement nobody can cite. |
| 42 | 2026-09-05 | Building the frame the market-versus-model regression de-vigs, 2 of 566,377 settled opinions had no complement at their own book, and the builder refused to write rather than drop them — by design, because a silently dropped row is a book that quietly stops counting. Refuse the whole measurement over 0.00035% of it, or exclude them? | **Exclude, with a counted and reconciling census, above a declared threshold that still refuses.** Diagnosed first, because a threshold that hides a join defect is the worst outcome: BetMGM hung `alternate_spread home +18.5` at -110 on 2024-02-17 and no away side at that rung anywhere in the store, and DraftKings hung `team_total home_over 74.5` at -105 on 2024-11-24 while its own book carried no under, though two other books did. Both are genuine one-sided quotes, not a failed join, and the de-vig must stay within a book because pairing across books understates the hold. | A wager whose book hung one side supplies no hold and cannot be de-vigged; excluding it is arithmetic. Excluding it silently is the defect the refusal exists to prevent, so the count is stated wherever the population is stated, and a large share still refuses — a big loss is a broken join wearing the costume of a data quirk. |
| 43 | 2026-09-05 | The player-prop design names 33 pre-registered hypotheses — ten priceable markets by three conference tiers against the de-vigged two-sided fair price, plus one per tier against an identity-blind role-prior control. Registering them takes the ledger from 62 cumulative hypotheses to 95 and the Bonferroni factor from **x1.7095 to x1.7689** (computed here, not quoted from the design, which had the pair right). That cost is paid by every interval this lab has already published. Register before the model exists, or wait until there is something to measure? | **Register now, and publish what it costs.** The 33 are appended before a line of the model is written and before any player number is measured; `tests/test_the_player_props_are_pre_registered.py` asserts against the tree that none of the model's files exists yet, so the ordering is a fact about the commit rather than a claim in a docstring. Two published verdicts move at the wider factor and both are reported: **low-major tier ROI**, -4.0% over 59,475 bets, was a demonstrated deficit at [-8.0%, -0.1%] and is **no demonstrated edge** at [-8.1%, +0.0%] — it crosses at the 85th hypothesis; and the replication's **total_points / mid_major held-out** cell, -6.4% over 8,214 bets, was a demonstrated deficit at [-12.5%, -0.2%] and is **no demonstrated edge** at [-12.7%, +0.0%] — it crosses at the 95th, which is this registration's last entry. Seven blind null-baseline sides in the price backtest lose their demonstrated-deficit reading too (79 of 190 become 72). Mid-major survives: -4.3% over 88,344 bets, [-8.2%, -0.4%], and it would take 411 hypotheses to widen it across. Nothing anywhere becomes an edge. | A direction declared after the numbers are seen is not a prediction, and the design's own expected result — no demonstrated edge in any cell — is worth nothing unless it was written down first. The cost is the point rather than an objection to it: an interval is paid for by the whole search that produced it, including the parts that have not run yet, and a claim that dissolves once the search is counted in full was never worth the width it was first printed at. The alternative, registering after the run, buys the same 33 degrees of freedom and pretends they were free. |
| 44 | 2026-09-05 | The same design names seven quantities as **descriptive-only**: the over/under split inside every cell, calibration by \|z\| bucket, `mean(actual)/mean(mu)` and its matched non-quoted arm, the refusal census, the minutes-projection fit, the half-life robustness row, and the unapplied diagnostic columns. They cost no hypotheses, and the design says they may never be promoted to a finding. Record them in the ledger, or leave the promise in the design? | **Record them in the ledger, with the refusal enforced in three places.** `ExperimentLedger` gained a `descriptive_only` list that is excluded from `count` — so the correction is still 95, not 102 — and `record()` raises `PromotionRefused` for any hypothesis carrying a declared `(search, name)`. `save()` refuses to drop a declaration, because deleting the declaration is how the in-process refusal would be got around, and `scripts/check_ledger_append_only.py` checks both at the PR diff, reading the **base**'s declarations as well as the head's so that removing the declaration and adding the hypothesis in one commit is still caught. `tests/test_descriptive_only_cannot_be_promoted.py` runs all three, including that combined edit. | The exemption is correct — none of the seven is a claim about edge, and charging Bonferroni for seven descriptions would widen every real interval in this lab for no protection. But the exemption is only honest while it runs both ways, and the moment somebody wants to promote a diagnostic is the moment it came out flattering. A promise in a design document is not a guard, and this ledger's whole argument is that the tests which failed are what make a survivor unlikely to be chance. |
| 45 | 2026-09-05 | `reports/why_the_model.py` prints a retraction paragraph when the low-major claim no longer holds, and that paragraph was one fixed sentence: *"Nothing about the model changed. The population did."* On 2026-09-04 that was true. Today low-major crosses zero on a record whose population, model and store are byte-identical to yesterday's — only the family size changed. Leave the sentence, or derive it? | **Derive it from the row.** `_why_it_no_longer_holds` now reads the **uncorrected** interval: if it still excludes zero, the measurement did not move and the correction did, and the paragraph says so and names the factor and the cumulative count; if it spans zero on its own, something upstream moved and the function says it cannot tell what, because the record it reads cannot. Both branches are exercised in `test_the_retraction_reads_its_current_figure_from_the_record`. | A generated document that states a cause it cannot read is the same defect as one that types a figure it cannot re-derive — and this file exists because that document once carried typed figures under a heading promising it could not. The fixed sentence would have printed a false cause today, in the one paragraph whose whole job is to explain why a claim was withdrawn. |
